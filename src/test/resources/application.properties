# 测试环境默认配置（所有测试共用）
# 仅设置与主配置冲突或测试必须覆盖的项，避免主配置在测试中导致 Mongo 校验失败

# MongoDB：测试环境必须使用 uri，否则 Spring Boot 2.6 在合并主配置的 host/port 后仍可能校验失败
spring.data.mongodb.uri=mongodb://localhost:27017/fleets_im_test

# 禁用 Arthas，避免绑定 telnet/http 端口导致测试上下文启动失败
spring.arthas.enabled=false
